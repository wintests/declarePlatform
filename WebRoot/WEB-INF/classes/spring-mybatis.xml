<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<settings>
        <!-- 打印查询语句 -->
        <setting name="logImpl" value="STDOUT_LOGGING" />
    </settings>
	
	<!-- 自定义别名 -->
	<typeAliases>
		<package name="com.qjz.declarePlatfrom.domain"/>
	</typeAliases>
	
	<!-- 配置mapper
		  由于使用spring和mybatis的整合包进行整合，这里无需配置
		  但必须遵循：mapper.java接口文件和mapper.xml文件同名且在同一目录
	-->
    <!--<mappers></mappers>  -->
</configuration>