<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper    
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"    
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace:命名空间,对statement的信息进行分类管理 -->
<!-- 注意：在mapper代理时，它具有特殊及重要的作用,需要和mapper接口的全限命名一致 -->
<mapper namespace="com.qjz.declarePlatfrom.dao.ApplyDao">
	
	<select id="listApply" resultType="com.qjz.declarePlatfrom.domain.Apply">
		SELECT * FROM apply
	</select>
	
	<select id="count" resultType="java.lang.Long">
		SELECT count(*) FROM apply
	</select>
	
	<update id="updateApply" parameterType="com.qjz.declarePlatfrom.domain.Apply">
		UPDATE apply
		SET item_name = #{item_name},
			item_type = #{item_type},
			item_user = #{item_user},
			item_department = #{item_department},
			item_starttime = #{item_starttime},
			item_deadline = #{item_deadline},
			item_submit = #{item_submit},
			apply_time = #{apply_time},
			item_status = #{item_status},
			item_description = #{item_description}
		WHERE item_id = #{item_id},
	</update>
	
	<insert id="addApply" parameterType="com.qjz.declarePlatfrom.domain.Apply">
		INSERT INTO
		apply(item_name,
			item_type,
			item_user,
			item_department,
			item_starttime,
			item_deadline,
			item_submit,
			apply_time,
			item_status,
			item_description)
		VALUES(#{item_name},
			#{item_type},
			#{item_user},
			#{item_department},
			#{item_starttime},
			#{item_deadline},
			#{item_submit},
			#{apply_time},
			#{item_status},
			#{item_description})
	</insert>
	
	<delete id="deleteApplyById" parameterType="int">
		DELETE FROM apply
		WHERE item_id = #{item_id}
	</delete>
	
	<delete id="deleteApplyBatchs">
		DELETE FROM apply
		WHERE item_id IN
		<foreach collection="array" item="item_id" open="(" close=")" separator=",">
			#{item_id}
		</foreach>
	</delete>
	
	<update id="submitApply">
		UPDATE apply
		SET item_submit = #{item_submit}
		WHERE item_id = #{item_id}
	</update>
	
	<update id="submitApplyBatchs">
		UPDATE apply
		SET item_submit = #{item_submit}
		WHERE item_id IN
		<foreach collection="array" item="item_id" open="(" close=")" separator=",">
			#{item_id}
		</foreach>
	</update>
	
</mapper>